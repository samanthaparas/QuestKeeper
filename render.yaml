services:
  - type: web
    name: questkeeper-api
    runtime: node
    plan: free
    rootDir: questkeeper-backend
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /

  - type: web
    name: questkeeper
    runtime: static
    rootDir: questkeeper-frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_BASE_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
